-- Documentación: módulo `ftplugin/rust.lua`.
-- Propósito: ajustes locales para buffers Rust sin afectar otros filetypes.

require('language.rust.whichkey').setup()

require('tools.neogen-whichkey').setup()
